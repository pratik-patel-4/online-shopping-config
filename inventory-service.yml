server:
  port: 8085

spring:
  application:
    name: inventory-service
  datasource:
    url: jdbc:postgresql://localhost:5432/inventorydb
    username: postgres
    password: admin@123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
